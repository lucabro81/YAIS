export enum ElemPosition {
    BEFORE = 0,
    AFTER = 1
}

export enum Settings {
    BOTTOM_REACHED = 0,
    CONTAINER = 1,
    CLASS_ITEM = 2,
    DATA = 3,
    ITEMS_PER_PAGE = 4,
    ITEM_TEMPLATE = 5,
    LOOP = 6,
    TOP_REACHED = 7,
}